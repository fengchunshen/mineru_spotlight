# MinerU Tianshu Requirements
# 天枢项目依赖

# Core MinerU
mineru[core]>=2.5.0

# Image Augmentation (Version Pinned for Compatibility)
albumentations>=1.4.4,<2.0.0
albucore>=0.0.13,<0.0.20

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# LitServe for GPU Load Balancing
litserve>=0.2.0

# Model download (ModelScope fallback for HF)
modelscope>=1.22.1

# PyTorch (GPU) — 请按官方索引单独安装，不在此文件自动安装
# 示例（CUDA 12.1）:
# pip install --force-reinstall --no-cache-dir --index-url https://download.pytorch.org/whl/cu121 torch torchvision

# Async HTTP Client
aiohttp>=3.11.0

# Logging
loguru>=0.7.0

# Office Document Parsing
markitdown[all]>=0.1.3

# MinIO Object Storage
minio>=7.2.0

# Optional: For better performance
# ujson>=5.10.0

